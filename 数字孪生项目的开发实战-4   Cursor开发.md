好的，我将根据您新增的需求，对之前的 Cursor 专用开发提示词进行更新和完善：

```
# 船舶/工业设备数字孪生监控平台开发指南 (Cursor 专用)

## 项目概述

开发一个高性能、用户友好的船舶/工业设备数字孪生监控平台，通过精确的 3D 可视化实时呈现设备状态，实时监控关键运行参数，并集成智能故障诊断与预警功能，旨在帮助运维人员提前发现潜在问题，显著降低设备故障率，并最终延长设备使用寿命。

## 目标用户

设备运维人员、技术工程师、管理决策层

## 核心功能模块

1.  **数字模型可视化模块**
    * 3D 模型展示 (支持缩放、旋转、平移)
    * 设备状态映射 (颜色、动态效果)
    * 分层展示 (外观、内部结构、组件)
    * 多维度视图 (2D 剖面、3D 立体、爆炸图)
    * **部件列表面板**
        * 显示所有部件名称
        * 支持部件名称搜索
    * **部件悬停与选择**
        * 部件悬停高亮 (自定义高亮材质)
        * 部件选中 (自定义选中材质)
    * **部件详情面板**
        * 显示选中部件的详细信息 (名称、型号、参数等)
    * **平滑摄像机转换与聚焦**
        * 实现摄像机平滑过渡效果
        * 支持自动聚焦到选定部件
    * **模型拆解与合并**
        * 支持模型部件的拆解和合并
        * 可控制拆解动画

2.  **实时监控模块**
    * 参数实时监测 (温度、压力、流量、振动)
    * 阈值预警机制 (可配置)
    * 趋势分析图表
    * 运行状态总览 (评分、健康指数)

3.  **智能诊断模块**
    * 故障检测识别 (规则/AI)
    * 故障定位与分级
    * 故障原因分析与建议
    * 专家知识库

4.  **预测性维护模块**
    * 设备健康评估
    * 寿命预测
    * 维护计划推荐
    * 维护效果评估

5.  **协同工作模块**
    * 工单管理
    * 专家远程协助
    * AR 辅助维修指导
    * 多终端协同 (PC、平板、手机)

## 技术栈

* 前端框架：Vue 3 + Composition API
* 3D 引擎：Three.js (r150+)
* 状态管理：Pinia
* 数据可视化：ECharts 5
* 通信协议：WebSocket + RESTful API
* 构建工具：Vite
* UI 组件库：Element Plus / Ant Design Vue

## 架构设计

* **分层设计**：数据采集层、数据处理层、应用服务层、展示交互层
* **模块化架构**：3D 渲染引擎、业务逻辑、数据处理解耦
* **3D 引擎核心**：自定义渲染引擎、ECS 架构、场景图管理、自定义着色器
* **数据流**：单向数据流，状态变更 -> 更新 3D 模型 -> 刷新 UI

## 开发重点与优化

* **3D 模型优化**
    * 几何体简化、LOD、模型压缩 (glTF, Draco)、网格合并、剔除
    * Cursor 提示：  "逐步优化模型加载，先使用 SimplifyModifier 减少面数，然后实现 LOD 切换，并采用 glTF 格式和 Draco 压缩。"
* **材质优化**
    * 纹理优化、材质复用、避免复杂 Shader、**自定义高亮/选中材质**
    * Cursor 提示：  "将 MeshPhongMaterial 替换为 MeshStandardMaterial，并优化其金属度和粗糙度设置。为部件悬停和选中状态创建单独的材质。"
* **渲染优化**
    * 延迟渲染、后处理优化 (Bloom, 抗锯齿)、阴影优化、透明度处理
    * Cursor 提示：  "分析并优化后处理效果的参数，例如 Bloom 的强度和半径。"
* **实时数据更新优化**
    * WebSocket、数据格式优化 (JSON)、数据压缩、数据过滤与节流、局部更新
    * Cursor 提示：  "使用 WebSocket 进行实时数据传输，并实现数据节流，避免频繁更新 3D 场景。"
* **前端架构优化**
    * 代码模块化/组件化、懒加载、代码压缩/混淆、缓存、Web Workers
    * Cursor 提示：  "将 Three.js 场景初始化、模型加载、事件处理等逻辑拆分成独立的 Vue 组件或 JavaScript 模块。"
* **用户体验优化**
    * 加载反馈、交互优化、响应式设计、**平滑摄像机动画、部件信息展示**
    * Cursor 提示：  "为模型加载过程添加加载动画和进度条。实现点击部件列表项时，摄像机平滑移动并聚焦到对应部件。"

## 开发流程

1.  基础架构搭建
2.  核心 3D 引擎服务开发
3.  设备状态管理系统实现
4.  用户界面组件开发 (**包含部件列表、详情面板**)
5.  性能优化与测试
6.  文档与部署

## 质量标准

* FPS 稳定在 60+
* 首屏加载时间 < 3 秒
* 交互响应时间 < 100ms
* 内存占用 < 500MB

## 技术实现关键点

* Three.js 与 Vue 集成
* 模型处理最佳实践 (glTF, Draco)
* 数据实时性保障 (WebSocket)
* 可访问性与易用性
* **高效的部件选择与信息展示**
* **平滑的摄像机控制**
* **灵活的模型拆解与合并**

## 提示 Cursor 的一般建议

* 逐步提问，明确意图
* 提供代码片段和上下文
* 保持耐心和清晰的沟通

这个更新后的提示词更全面地包含了您新增的需求，希望能帮助您更高效地使用 Cursor 进行开发。